# memassist 프로젝트 정체성

memassist는 프로젝트 로컬 메모리 RAG 시스템이다.

memassist의 목적은 사용자 선호, 결정, 작업 방식, 프로젝트 맥락을 로컬 메모리로 저장하고, 현재 요청과 관련된 메모리를 검색해 활성 AI 코딩 에이전트의 컨텍스트로 주입하는 것이다.

memassist는 자동 정책 엔진이 아니다. 저장된 메모리를 숨은 승인 게이트, 도구 차단 규칙, 자율 강제 규칙, 권한 결정으로 컴파일해서는 안 된다. 검색된 메모리는 더 나은 판단을 위한 컨텍스트이며, 현재 사용자 요청을 해석하고 행동하는 책임은 활성 AI 코딩 에이전트에게 있다.

## 핵심 사이클

```text
사용자 요청 시작
-> 기존 메모리 검색
-> 관련 메모리 컨텍스트 주입
-> 에이전트 작업 진행
-> 턴 종료
-> 이번 입력에 새 메모리 후보가 있는지 판단
-> 로컬 메모리 저장
-> 다음 턴부터 검색 가능
```

## 설계 원칙

- 메모리는 정책이 아니라 컨텍스트다.
- `UserPromptSubmit`은 읽기 경로다. 기존 메모리를 검색하고 관련 컨텍스트를 주입한다.
- `Stop`은 쓰기 경로다. 턴이 끝난 뒤 새 메모리 후보를 판단하고 저장한다.
- 새로 저장된 메모리는 생성된 턴이 아니라 이후 턴부터 영향을 준다.
- 메모리는 `source_quote`, `source_ref` 같은 출처 근거를 보존해야 한다.
- 로컬 Markdown 메모리 artifact는 사람이 읽고 수정할 수 있는 메모리 기록의 원본이다.
