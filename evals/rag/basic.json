{
  "cases": [
    {
      "query": "change session timeout and verify",
      "seed": [
        {
          "section": "context",
          "type": "lesson",
          "content": "Session timeout fixes should update the session timeout behavior only.",
          "tags": ["auth", "session", "timeout"],
          "paths": ["src/auth/session.py"],
          "source_quote": "Session timeout fixes should update the session timeout behavior only.",
          "source_ref": "evals/rag/basic.json#case-1-context"
        },
        {
          "section": "verifier",
          "type": "workflow",
          "content": "Run npm test -- auth session after session timeout changes.",
          "tags": ["verification", "test", "auth", "session"],
          "source_quote": "Run npm test -- auth session after session timeout changes.",
          "source_ref": "evals/rag/basic.json#case-1-verifier"
        }
      ],
      "expect": [
        { "term": "npm test", "section": "verifier" },
        { "term": "session timeout", "section": "context" }
      ],
      "forbid": [
        { "term": "refresh token", "section": "context" }
      ]
    },
    {
      "query": "edit refresh token policy",
      "seed": [
        {
          "section": "context",
          "type": "rule",
          "content": "Ask before editing refresh token behavior; this is agent context, not a tool block.",
          "tags": ["auth", "security", "refresh", "token", "directive"],
          "paths": ["src/auth/refresh-token-policy.ts"],
          "caution_level": "none",
          "source_quote": "Ask before editing refresh token behavior.",
          "source_ref": "evals/rag/basic.json#case-2-context"
        }
      ],
      "expect": [
        { "term": "refresh token", "section": "context" }
      ],
      "forbid": [
        { "term": "temporary branch", "section": "*" }
      ]
    }
  ]
}
