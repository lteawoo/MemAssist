{
  "cases": [
    {
      "name": "verification workflow is remembered without policy noise",
      "query": "session timeout npm verification",
      "expect": ["npm test"],
      "forbid": ["refresh token"],
      "seed": [
        {
          "type": "workflow",
          "content": "Run npm test -- auth session after session timeout changes.",
          "tags": ["verification", "test", "auth", "session", "timeout"],
          "status": "long_term",
          "importance": 0.8
        },
        {
          "type": "preference",
          "content": "Keep refresh token policy changes as an internal blocked memory policy.",
          "tags": ["auth", "token"],
          "status": "candidate",
          "importance": 0.9
        }
      ],
      "limit": 5
    },
    {
      "name": "policy memory is retrieved only when relevant",
      "query": "refresh token protected path",
      "expect": ["refresh token"],
      "forbid": ["temporary branch"],
      "expect_statuses": ["block_policy", "warn_policy", "durable", "long_term", "active", "auto_active", "pinned"],
      "seed": [
        {
          "type": "rule",
          "content": "Refresh token policy edits are blocked by autonomous memory policy.",
          "tags": ["auth", "security", "refresh", "token", "policy"],
          "paths": ["src/auth/refresh-token-policy.ts"],
          "status": "block_policy",
          "enforcement": "block",
          "importance": 0.9
        },
        {
          "type": "workflow",
          "content": "Delete temporary branch after release cleanup.",
          "tags": ["git", "cleanup"],
          "status": "candidate",
          "importance": 0.4
        }
      ],
      "limit": 5
    }
  ]
}
